--- Client.C.orig
+++ Client.C
@@ -833,6 +833,9 @@
 {
     unsigned char *p;
 
+    // Property accessors must not truncate pointer-width values to int
+    // (x86_64 fix; former getAtomProperty returned long, not int).
+
     // no -- allow any type, not just string -- SGI has "compound
     // text", and part-garbage is possibly better than "incognito" --
     // thanks to Bill Spitzak
