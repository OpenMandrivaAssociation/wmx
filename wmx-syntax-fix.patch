--- Menu.C.orig
+++ Menu.C
@@ -719,6 +719,7 @@
 CommandMenu::CommandMenu(WindowManager *manager, XEvent *e, char* otherdir)
     : Menu(manager, e)
 {
+    // Default arguments belong on the declaration only (not the definition).
     const char *home = getenv("HOME");
     const char *wmxdir = getenv("WMXDIR");
 
